# CIVO API token (required for OpenTofu deploy)
# Get from: https://dashboard.civo.com/security
TF_VAR_civo_token=

# GitHub Actions also needs these as repository secrets:
#   CIVO_TOKEN - Same as above, used by deploy workflow
